name: CI

on:
  push:
    branches: [main]
  pull_request:
  merge_group:
  schedule:
    - cron: '0 6 * * 1'

permissions: {}

# This workflow runs the extension's own test matrix and is the ONE per-extension
# CI value: its `with:` block differs per extension and is listed under
# intentional-drift. The security/quality jobs with elevated permissions are
# drift-enforced in checks.yml; do not duplicate them here.
jobs:
  ci:
    uses: netresearch/typo3-ci-workflows/.github/workflows/ci.yml@main
    permissions:
      contents: read
    with:
        php-versions: '["8.2","8.3","8.4","8.5"]'
        typo3-versions: '["^13.4","^14.3"]'
        typo3-packages: '["typo3/cms-core", "typo3/cms-fluid", "typo3/cms-scheduler"]'
        run-functional-tests: true
        functional-test-db: 'sqlite'
        run-fractor: true
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}